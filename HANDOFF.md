# Tooling handoff

Continue the user-authorized play-and-enhance objective. Shared guidance owns tooling status only; select the run and read its current strategy/issues for identities, orders, discoveries and permissions. A lab milestone or stable colony is not victory.

Current0.5.0 simplifies ordinary operation after fresh-agent A/B trials. Read docs/PLAY-LOOP-EXPERIMENT.md for measured results and limits. Direct MCP used fewer calls and modestly more game time, but scenarios differed and neither trial established sustainable colony growth, combat skill or a better win rate. Full original traces and audits remain run-local.

Normal startup no longer requires STATE/full packets. Ordinary call/batch requests are automatically journaled without unfinished-goal records or intent text by default. Use act, --track or explicit checks for strategic outcome tracking. Unknown operations still block replay; ownership, identity and pause contracts remain. HP improvement is not automatically critical damage; new conditions and unfamiliar shapes stay visible for review.

180offline tests pass. Independent review found one leftover STATE instruction; corrected it and added CLI/check-implied tracking coverage. A bounded live check now passed: ordinary CLI pause, work-priority and queued-job calls needed no intent/new goal records; two supervised hours returned paused. Cooking completion remained unverified from actual job/resource reads. This is functional evidence, not a speed result. Production was frozen at a247fef during the two experimental trials; these default changes came afterward.

Use the pinned pyenv runtime. API index docs/api/README.md routes all113captured tools without routine full-catalog loading. Shared mechanics and campaign learning remain separate. Retrieve relevant details before consequential decisions; concise startup does not authorize ignoring missing facts.

Next: follow PLAN.md's remediation sequence. First isolate host/connection versus decision-cycle costs in a bounded diagnostic; then change one demonstrated bottleneck and run frozen fresh-agent/resume experiments. Do not add another framework, treat each minor fix as success, or use more gameplay ticks as a substitute for good colony decisions. Current run is safely paused with its own handoff; retrieve its exact state there before live control.
